This is the README file for the multicore version of the Chronos timing analyzer.

Source code
============
Source code of the shared cache and bus is in directory "m_cache"

To compile the code
======================
./configure with required arguments (see ./configure --help)
./make
It should produce a binary file named "m_cache/opt"

To run the code
======================================================================
You can use the bash scripts given in the "scripts folder"
